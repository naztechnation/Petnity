enum RouteStyle { material, cupertino, fadeIn, slideIn, slideUp }

enum ImageType { svg, network, asset, file }
enum Status { uninitialized, authenticateError, authenticateCanceled, authenticated, authenticating }

enum MessageType { success, error, progress }
enum UploadType {none, upload1, userPhoto, photoId }

enum DropdownType { form, button, menu }

enum PetGenderType { none, male, female }

enum PetTypes { none, dog, squirrels, cats, monkeys, parrot, birds, rabbits }
enum PetBreedsTypes { none, german , bulldog, chihuahua, poodle, maltipoo, bullmastiff, alstatians }

enum FilterStatus { request, chats }

enum GalleryRating { gallery, ratings }

enum ViewState { idle, loading, processing, success, failed }

enum sessionMedium { chat, call, video }

enum UserType {none, user, serviceProvider}

enum Services {
  none,
  dogwalkers,
  petdate,
  dogsitters,
  trainer,
  vets,
  grooming,
  breeders,
  petsellers,
  boarding,
  petcaregivers
}
